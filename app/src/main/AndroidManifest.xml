<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sportmagazine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcherf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MostrarNoticia"></activity>
        <activity android:name=".Tienda" />
        <activity android:name=".ListaNoticias" />
        <activity android:name=".MenuTool" />
        <activity android:name=".MenuActivity" />
       <activity android:name=".GymActivity" />
        <activity android:name=".MarketPlace" />

        <activity android:name=".ItemGym" />

        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>